# LitAgents v9

LitAgents orchestrates autonomous AI literary agents to manage the entire novel-writing workflow, from authoring to translation and management.

## Run & Operate

To run the application locally, ensure you have Node.js installed.

```bash
# Install dependencies
npm install

# Run database migrations
npx drizzle-kit push:pg

# Start the development server
npm run dev
```

**Environment Variables:**

- `DATABASE_URL`: Connection string for your PostgreSQL database.
- `DEEPSEEK_API_KEY`: API key for DeepSeek V4-Flash models.
- `FISH_AUDIO_API_KEY`: API key for Fish Audio TTS service.

## Stack

- **Frameworks**: React, Express
- **Runtime**: Node.js
- **Language**: TypeScript
- **ORM**: Drizzle ORM
- **Validation**: Zod
- **Build Tool**: Vite (client), esbuild (server)
- **UI**: shadcn/ui (Radix UI, Tailwind CSS)
- **Routing**: Wouter
- **State Management**: TanStack Query
- **AI Models**: DeepSeek V4-Flash (all agents)

## Where things live

- **Database Schema**: `shared/schema.ts`
- **API Endpoints**: `server/routes.ts`
- **AI Agents**: `server/agents/`
- **Orchestration Logic**: `server/orchestrator.ts`
- **Frontend Pages**: `client/src/pages/`
- **Design System/Styling**: `client/tailwind.config.ts`, `client/src/index.css`
- **World Bible Management**: `server/utils/world-bible-format.ts`
- **Series Milestones/Threads Extractor**: `server/utils/series-milestones-extractor.ts`
- **AI Usage Recording Helper**: `server/utils/ai-usage.ts`

## Recent fixes

> Historial completo en `CHANGELOG.md`. Aquí solo los fixes aún recientes.

- **[Fix151][Puerta 6] Auditor Estructural degradado a ADVISORY: cierra el rediseño 100% autónomo y elimina la dependencia de `awaiting_structural_guidance`**: última pieza del rediseño de calidad 100% autónomo (orden P1→P4→P5→P0→P6; sin guía humana — el usuario no técnico no lee escaletas). El Auditor Estructural DETERMINISTA (mide tokens/etiquetas, no SEMÁNTICA) era el gatekeeper final del diseño: si tras todo el bucle SA (`outerSALoop`, 8 iter + audit on-demand WB + auto-guidance Fix118) el mejor score quedaba <`MIN_PUBLISHABLE_SA_SCORE` (7) o una dim crítica de 2ª mitad seguía KO, ponía `status="awaiting_structural_guidance"` + `pendingStructuralGuidance` y hacía `return`, BLOQUEANDO el Narrador a la espera de guía MANUAL. Esto contradice el objetivo autónomo y por FALSOS negativos del detector atascaba novelas horas (Fix145/146). Como P1/P4/P5 ya garantizan la calidad real (agencia del clímax en plan y prosa + lectura final por ejes), el determinista ya no necesita bloquear. **Arreglo** (`server/orchestrator.ts`): el bloque del gate pasa a ADVISORY — cuando el best queda bajo umbral o hay KO crítico, NO toca el status, NO hace `return`, restaura `worldBibleData=bestSAOverall.data` (mejor escaleta, anti-regresión), loguea `console.warn`+`activityLog` warning y fluye al Lector Beta + Narrador. Se conservan el bucle SA y su auto-guidance Fix118 (mejora autónoma; solo deja de bloquear) y `faChronicSoleBlocker` (Fix145-B). **Compatibilidad**: la ruta de RESUME (`_generateNovel` reusa snapshot; endpoint `POST /api/projects/:id/structural-guidance`) queda INTACTA para recuperar proyectos YA atascados; simplemente ninguna ruta vuelve a ENTRAR en ese estado. **Post-review**: `console.warn` de trazabilidad para el borde `bestSAOverall===null` y limpieza de textos residuales "gate human-in-the-loop". Ningún setter de `awaiting_structural_guidance` activo en runtime (verificado). Footer `v9.4.4`→`v9.4.5`. Coste 0 LLM. Sin migración. tsc PASS. Architect PASS.

- **[Fix150][Puerta 0] Forjador de Concepto: eleva la premisa cruda a un CONCEPTO RECTOR fuerte ANTES de diseñar nada**: sexta y última pieza del rediseño de calidad 100% autónomo (orden P1→P4→P5→P0; sin guía humana). Las puertas previas arreglan la AGENCIA (plan/prosa) y los EJES globales de la novela ya escrita, pero todas operan sobre una base que arrancaba de la premisa CRUDA del autor — a menudo genérica o cliché. Ningún paso elevaba esa idea a un concepto de alto techo antes de que el Arquitecto la convirtiera en estructura. **Arreglo**: nuevo agente `server/agents/concept-forge.ts` (`ConceptForgeAgent`, "Director Creativo", DeepSeek V4-Flash con thinking). Regla inviolable: ELEVAR, NUNCA SUSTITUIR — respeta género/tono/idea central/personajes/escenario declarados; solo afila y profundiza. Maximiza 5 ejes (originalidad, especificidad, motor_dramatico, columna_tematica, gancho) y emite un `concepto` rector de 250-450 palabras (logline + deseo/necesidad/herida del protagonista + motor de conflicto + antagonismo espejo + mundo + columna temática + 2-4 "promesas al lector"), con autocrítica honesta (`puntuacion_concepto`, `veredicto`, `ejes`, `debilidades`). Saneamiento defensivo; `repairJson` ya devuelve objeto. Exportado en `index.ts`. **Bucle AUTÓNOMO** `runConceptForgeGate` (`MAX=3`): sale por CALIDAD (`puntuacion_concepto>=8` Y apto Y ningún eje <6); ante estancamiento auto-escala la audacia reinyectando el concepto previo + debilidades; conserva el mejor concepto visto. **Enganche**: en `_generateNovel`, justo donde se computa `effectivePremise` (solo standalone — guard por SEMÁNTICA real `!project.seriesId`, NO por `seriesContextContent` poblado, que estaría vacío en el vol. 1 de serie o si falla la carga; en serie el concepto se gobierna a nivel de saga, comportamiento INTACTO); el concepto forjado se antepone como bloque "CONCEPTO RECTOR" seguido de la premisa original del autor, y fluye al WBA (Fase 1) y al Arquitecto. **Post-review**: el bloque usa wording "vinculante" solo si el concepto alcanzó el umbral de calidad; si no, se inyecta como "orientativo" (eleva el suelo sin forzar un concepto autoevaluado flojo por encima de la premisa, que SIEMPRE se conserva debajo). **Best-effort** en `try/catch`: si el agente falla, se usa la premisa original (sin bloquear). NUNCA `awaiting_structural_guidance`. Footer `v9.4.3`→`v9.4.4`. Coste: ≤3 lecturas LLM ligeras pre-generación. Sin migración. tsc PASS. Architect PASS.

- **[Fix149][Puerta 5] Lectura Final por Ejes (LFE) + series-awareness del auditor de arco secundario**: quinta puerta del rediseño de calidad 100% autónomo (orden P1→P4→P5; sin guía humana). Ningún paso autónomo y vinculante leía la novela ENTERA ya escrita para auditar los cuatro ejes globales que más rompen al lector: **promesa→pago** (cabos sueltos/Chéjov), **coherencia causal global** (giros sin sembrar, conveniencias), **consistencia de personaje** (motivacional) y **cierre temático**. **Arreglo**: nuevo agente `server/agents/final-axis-reader.ts` (`FinalAxisReaderAgent`, espejo de la Puerta 4) que lee todos los caps `completed` (recorte cabeza+tail) y devuelve JSON `puntuacion_global`/`veredicto`/`ejes{promesa_pago,coherencia_causal,consistencia_personaje,cierre_tematico}`/`resumen`/`problemas[{numero,eje,severidad,descripcion,directiva_de_reescritura}]` (saneamiento defensivo; `repairJson` ya devuelve objeto). Exportado en `index.ts`. **Bucle AUTÓNOMO** `runFinalAxisReadGate` (enganchado en `_generateNovel` y `_resumeNovel` justo tras `runProseAgencyGate` y antes de `finalizeCompletedProject`, rama aprobada; requiere `novela.length>=5`): apto si `puntuacion_global>=7` Y sin crítica/alta; si no, reescribe cada cap problemático vía `rewriteChapterForQA(..., "editorial", directiva)` (Cirujano→Narrador), recarga y re-juzga; auto-escala intensidad; conserva la mejor versión. **Fail-safe determinista** (sin humano): tras `MAX_PASSES=3` sin apto, fuerza la reescritura del PEOR cap y hace UNA re-lectura con patrón "revertir por defecto" (`mustRevert=true`, `isBetter`: menos críticos/altos > score); cualquier camino límite revierte; `restoreSnapshot` fuerza `status:"completed"`. **NUNCA `awaiting_structural_guidance`**; todo best-effort en `try/catch`. **Series-awareness de `auditArcoSecundario` (hallazgo del review)**: el detector asumía cierre del arco DENTRO del volumen, penalizando arcos de saga legítimos en volúmenes NO finales. Nuevo `SeriesAuditContext {isSeriesVolume,isFinalVolume}` threadeado por `runArchitectStructuralAudits`→`auditArcoSecundario` (ausente = standalone, INTACTO); en serie NO final se saltan `arco_secundario_abandonado`/`cierre_fantasma_secundario` y la subida a "alta" de `desaparicion_prolongada`, conservando las señales independientes del cierre como aviso (media). El contexto se computa UNA vez en `_generateNovel` (un solo `getSeries`): FINAL solo si se conoce el total y este volumen es el último (prequels nunca finales; total desconocido → conservador NO final). Footer `v9.4.2`→`v9.4.3`. Coste: ≤3 lecturas LLM de la novela completa + reescrituras acotadas; series-awareness 0 LLM. Sin migración. tsc PASS. Architect PASS.

- **[Fix148][Puerta 4] Editor de Prosa de Agencia: verifica EN LA PROSA escrita lo que la Puerta 1 garantizó en el plan**: segunda puerta del rediseño de calidad 100% autónomo (orden P1→P4). Espejo en PROSA de la Puerta 1: el Arquitecto puede sembrar la agencia en la escaleta y aun así el Narrador entregar un final NO ganado en la página (poder externo/secundario resolviendo, protagonista pasivo, cambio que se afirma pero no se ve en escena); ninguna revisión de prosa existente vigilaba la AGENCIA del clímax en el texto. **Arreglo**: nuevo agente `server/agents/prose-agency-editor.ts` (`ProseAgencyEditorAgent`), juez SEMÁNTICO de la prosa con la misma REGLA DE ORO que la P1 aplicada a lo escrito. Lee la prosa real de los caps del clímax (último ~30% + cualquiera con `mandato_agencia` estampado, ~9k chars/cap recorte cabeza+cola). Salida JSON: `puntuacion_agencia_prosa`, `veredicto`, `protagonista_es_agente_en_prosa`, `quien_resuelve_en_prosa`, `capitulos_problematicos[{numero, tipo, severidad, descripcion, directiva_de_reescritura}]`; saneamiento defensivo espejo Fix147; `repairJson` ya devuelve objeto. Exportado en `index.ts`. **Bucle AUTÓNOMO** `runProseAgencyGate` (enganchado en `_generateNovel` y `_resumeNovel` justo antes de `finalizeCompletedProject`, rama aprobada): apto si `score>=7` Y veredicto apto Y agente en prosa Y sin crítica/alta; si no, reescribe cada cap problemático vía `rewriteChapterForQA(..., "editorial", directiva)` (Cirujano→Narrador), recarga prosa, re-juzga; auto-escala intensidad ante estancamiento; conserva la mejor versión. **Fail-safe determinista** (sin humano): tras `MAX_PASSES=3` sin apto, fuerza una reescritura del PEOR cap con un MANDATO FINAL DE AGENCIA redactado por el editor (usa `quien_resuelve_en_prosa` + `getProtagonistName`), reduce la figura externa a ratificar. **NUNCA `awaiting_structural_guidance`**; toda la puerta best-effort en `try/catch`. Consume el `mandato_agencia` cableado en P1. **Anti-regresión de prosa (post-review)**: la puerta snapshotea/restaura la mejor versión de la PROSA (`{id,content,wordCount}`) con criterio compuesto `isBetter` (menos críticos/altos > protagonista agente en prosa > score); tras el fail-safe hace UNA re-lectura y aplica patrón "revertir por defecto" (`mustRevert=true`): solo conserva la reescritura forzada si es apta o estrictamente mejor; cualquier camino límite (clímax recargado vacío, juez sin veredicto, peor resultado) revierte a la mejor versión; `restoreSnapshot` también fuerza `status:"completed"` para que el capítulo revertido siga entrando en la comparación → garantía "jamás entregar peor versión" cerrada en todos los caminos. Footer `v9.4.1`→`v9.4.2`. Coste: ≤3 lecturas LLM + reescrituras acotadas. Sin migración. tsc PASS. Architect PASS.

- **[Fix147][Puerta 1] Editor de Desarrollo del plan + Regla de Agencia: garantiza ANTES de escribir que el protagonista se gana su clímax (no deus ex machina)**: primera puerta del rediseño de calidad 100% autónomo aprobado por el usuario (sin guía humana: el usuario no puede leer escaletas internas). Diagnóstico de fondo (caso "LUNA NEGRA" y recurrentes): las novelas salían con finales NO ganados — un poder externo (rey, juez, autoridad) o un secundario resolvía el conflicto central mientras el protagonista observaba pasivo. La dimensión determinista `deus_ex_machina` del Auditor Estructural mide tokens/etiquetas, no la SEMÁNTICA de quién resuelve; y los críticos semánticos existentes no vigilaban explícitamente la agencia del clímax. **Arreglo**: nuevo agente `server/agents/agency-critic.ts` (`AgencyCriticAgent`), juez del PLAN especializado en una sola cosa — la REGLA DE ORO (el conflicto central se resuelve en el clímax por una acción PROPIA del protagonista, sembrada antes; triunfa PORQUE HA CAMBIADO) y los 5 hitos del Plan Maestro aportado por el usuario (Incidente Incitador, Primer Giro, Punto Medio que vira pasiva→activa, Momento Oscuro, Clímax ganado por el cambio). Salida JSON: `puntuacion_agencia`, `veredicto` (apto/necesita_revision/reescribir), `protagonista_es_agente_del_climax`, `quien_resuelve_el_climax`, `hitos`, `problemas[{tipo: rescate_externo|protagonista_pasiva|climax_no_sembrado|punto_medio_sin_viraje|cambio_no_gana_el_climax|arco_no_pagado|acto2_plano, severidad, capitulos_afectados, descripcion, directiva_concreta}]`, `directivas_arquitecto`. Inyectado al Arquitecto vía nuevo campo `agencyFeedback` (espejo de `plotIntegrityFeedback`). **Bucle de convergencia AUTÓNOMO** en `server/orchestrator.ts` (tras el Auditor de Integridad, antes del determinista): sale por CALIDAD (`puntuacion_agencia>=7` Y veredicto apto Y `protagonista_es_agente_del_climax` Y sin `rescate_externo` crítica/alta), no por contador; ante estancamiento (score que no mejora) auto-escala la intensidad de la directiva en 2 niveles (enfático → el juez REDACTA la reescritura forzada del clímax usando `quien_resuelve_el_climax` y el nombre del protagonista) sin consultar a un humano; conserva la mejor escaleta vista. **Red dura determinista** (fail-safe, sin humano): si tras 4 pasadas la agencia no queda apta, restaura la mejor y estampa un MANDATO DE AGENCIA vinculante (`mandato_agencia`) en los caps del clímax (~últimos 30%) que viaja en la escaleta y honrarán las puertas de generación/prosa; el detector determinista de apoyo `detectExternalRescueSmell` (coocurrencia verbo-de-resolución + token de poder externo SIN el protagonista como actor en la zona de clímax) añade evidencia al log. **NUNCA usa `awaiting_structural_guidance`**. **Post-review**: el `mandato_agencia` ahora se PROPAGA por `SectionData` y los 3 constructores de secciones y se CONSUME en el prompt del Narrador (`ghostwriter.ts`, bloque de prioridad máxima) para que la red dura se materialice en la prosa; el retry de agencia pasa `reusePhase1Json` (reusa personajes/mundo, solo rehace la escaleta → más barato y converge mejor); saneamiento defensivo del JSON del juez (coacción de `capitulos_afectados` a array, severidad/hitos con defaults). Footer `v9.4.0`→`v9.4.1`. Coste: hasta 4 lecturas LLM del plan + reintentos del Arquitecto (calidad es prioridad #1, coste secundario; acotado por el presupuesto). Sin migración. tsc PASS. Architect PASS.

- **[Fix146] `arco_secreto` y `dosificación` dejan de generar falsos negativos/positivos que mantienen el score por debajo de 7 (mismo origen que Fix145)**: el usuario aportó el log real de "EL GRABADO DE LA LUNA NEGRA" tras la versión anterior; el diagnóstico fue que la novela llevaba ~16 h atascada NO solo por `falso_aliado` sino por VARIAS dimensiones a la vez: `arco_secreto` (casi siempre 2 problemas, cobertura 50-90%), `escalada_acto2` (1-4) y `dosificación` crónicamente baja (cobertura 15-47%). El usuario pidió investigar `arco_secreto` y `dosificación` como posibles falsos negativos (igual que `falso_aliado`) y hacerlos más tolerantes. **Arreglo (A) `arco_secreto` (`server/agents/scene-shape-auditor.ts`, `auditArcoSecreto`)**: fallback TOLERANTE antes de penalizar `siembra_textual_insuficiente_*` (espejo de Fix145). La siembra estricta exige tokens ≥4 chars del `hecho_revelado` en caps previos; cuando el Arquitecto SÍ siembra pero con sinónimos/tokens cortos (los logs muestran Fix117 auto-sincronizando 5-10 revelaciones por iteración → señal de que la siembra existe pero no casa token a token), daba falso negativo. Segunda pasada relajada: `extractSiembraTokens` parametrizado con `minLen` (3 en el fallback, captura ley/rey/mar/fe); un cap previo cuenta si tiene ≥1 token fuerte (no nombre propio), o ≥2 hits cualesquiera, o es un `setup_capitulo` DECLARADO con ≥1 hit de cualquier tipo (se confía en la declaración explícita del Arquitecto respaldada por una traza textual). Si la pasada relajada alcanza `minSiembra` → concede cobertura; además degrada de "alta" a "media" cuando hay AL MENOS una traza textual. **Control negativo**: un hecho sin ninguna traza en caps previos sigue penalizando "alta". **Arreglo (B) `dosificación` (`auditDosificacion`)**: denominador `expectsRev` más preciso. Antes contaba tipo J (confrontación, que puede ser pura acción sin revelación) y CUALQUIER cap con `eventos_pivotales>0` (casi todos) → inflaba el denominador, hundía la cobertura y disparaba un FALSO POSITIVO `dosificacion_no_declarada` (media, -0.7) iteración tras iteración. Ahora un cap "espera" revelación dosificable solo si es tipo M (revelación), si ya DECLARA `revelaciones_dosificadas`, o si su corpus contiene lenguaje de revelación inequívoco (`STRONG_REVELATION_HINTS`, sin "verdad"/"descubre" sueltos). Como `withRev ⊆ expectsRev`, la cobertura queda acotada a ≤1. **Control negativo**: un tipo M sin array declarado sigue contando como no-cubierto. Sandbox: arco positivo tolerante 100% / arco negativo 0% con 1 alta; dosif tolerante 100% sin falso positivo / dosif negativo 0% con 1 problema. Footer `v9.3.9`→`v9.4.0`. Coste 0 LLM (detectores deterministas). Sin migración. tsc PASS. Architect PASS.

- **[Fix145] `falso_aliado` deja de atrapar la novela en bucle por un falso negativo: detector del reveal del traidor más tolerante + red de seguridad en el gate**: la novela "EL GRABADO DE LA LUNA NEGRA" llevaba ~6 h atascada en auditoría estructural y se bloqueaba (`awaiting_structural_guidance`) pese a guía manual dos veces. La dimensión determinista `falso_aliado` (`server/agents/scene-shape-auditor.ts`, `auditFalsoAliado`) daba SIEMPRE 0% de cobertura; el audit on-demand del WBA confirmó WB suficiente (8/10) → FALSO NEGATIVO del detector (el Arquitecto sí declaraba el giro pero no con la estructura exacta esperada). Su único problema `reveal_no_declarado` (media, -0.7) dejaba el agregado <7 (`MIN_PUBLISHABLE_SA_SCORE`) iteración tras iteración. El usuario pidió **las dos cosas**. **Arreglo (A) detector más tolerante**: (1) nueva `STRONG_CORPUS_REVEAL_KEYWORDS`; (2) tokens de nombre relajados para nombres cortos (≥4 → ≥3 → token más largo ≥2); (3) en `revelaciones_dosificadas`, si hay UN solo traidor la keyword basta sin nombrarlo (≥2 traidores siguen exigiendo nombre); (4) fallback TOLERANTE por corpus antes de penalizar: si no hay reveal en la escaleta estructurada, busca en el último 40% (cap ≥ `minRevealCap`) co-ocurrencia de token del nombre + keyword fuerte en prosa y concede cobertura. Sandbox: prosa con keyword → 100%, nombre corto + revDos sin nombre (1 traidor) → 100%, control sin reveal → sigue 0% `reveal_no_declarado`. **Arreglo (B) red de seguridad en el gate (`server/orchestrator.ts`)**: `faChronicSoleBlocker` = agregado<MIN && sin KO crítico de 2ª mitad && todos los problemas FA son `reveal_no_declarado` && score sin la penalización FA (`10-2*altas-0.7*medias`, clamp[1,10]) ≥ MIN; si true se omite el gate (`&& !faChronicSoleBlocker`) y se continúa a generación con `console.warn` + `activityLog` warning (el Narrador debe materializar el giro en prosa). `falso_aliado` no está en `CRITICAL_SECOND_HALF_DIMS`. Footer `v9.3.8`→`v9.3.9`. Coste 0 LLM. Sin migración. tsc PASS. Architect PASS.

## Architecture decisions

- **Autonomous Agent Orchestration**: Uses a modular agent system (`BaseAgent` inheritance) for specialized tasks, managed by a central orchestrator, allowing for complex, multi-step literary workflows.
- **DeepSeek V4-Flash Context Window**: Leverages the 1M token context window of DeepSeek V4-Flash by injecting full previous chapter texts, entire previous volumes (for series), and pseudonym catalogs into agent prompts for enhanced coherence and reduced repetition.
- **Two-Step Editorial Workflow**: Editorial notes are first parsed and previewed (structured JSON output) before application, giving users control over AI-suggested changes and enabling a human-in-the-loop approval process.
- **Robustness and Auto-Recovery**: Includes mechanisms like best-effort buffers for agent outputs, automatic retry logic for failed generations (Architect, word count), persistence of partial progress (KDP pipeline, mid-generation checkpoints), and defensive filters for AI-generated instructions to prevent errors and ensure continuity.
- **Server-Sent Events (SSE) for Background Tasks**: Long-running AI operations (e.g., holistic review, editorial parsing) respond immediately with HTTP 202 and stream updates via SSE, preventing timeouts and keeping the UI responsive. Heartbeats are implemented to maintain connections.

## Product

- **Novel Generation**: Orchestrates AI agents for authoring new literary works.
- **Re-editing Pipeline**: Provides publication-quality re-editing with 12 specialized agents for deep analysis and correction.
- **Literary Adaptation & Translation**: Supports translation into 7 languages with a focus on publication-ready prose.
- **Series Management**: Features inter-book continuity, prequel/spin-off creation, and unified World Bible management across series volumes.
- **KDP Metadata Generation**: Automates the creation of Amazon KDP publishing metadata (descriptions, keywords, categories) for multiple markets.
- **Audiobook Generation**: Converts completed manuscripts into audiobooks using TTS, supporting pause/resume and parallel chapter generation.
- **Guide Workshop**: AI-powered generation of writing and style guides, including pseudonym-specific and series-coherence guides.
- **Back Matter System**: Configurable back matter pages (review requests, "also by" lists, author bios) for exports.
- **Manuscript Import**: Supports various file formats (`.docx`, `.txt`, `.md`) with intelligent chapter detection for re-editing or translation.

## User preferences

Preferred communication style: Simple, everyday language. Communicate ALWAYS in Spanish.

## Gotchas

- **KDP Pipeline Execution**: KDP pipeline runs in the background and persists progress incrementally. Partial failures are tolerated (market marked with `error`, pipeline continues).
- **Architect Timeout**: The Architect agent's phase 2 can take up to 18 minutes for large novels (post-[Fix18] extended schema); the orchestrator's frozen monitor (`HEARTBEAT_TIMEOUT_MS`) is set to 22 minutes, leaving a 4-min safety margin. Phase 1 keeps the default 12-min timeout.
- **Structural Notes Handling**: Editorial notes requesting structural changes (delete/merge chapters) are explicitly *not* auto-applied but logged as administrative actions requiring manual confirmation, preventing accidental data loss or corruption.
- **SSE Connection Stability**: Long-running background processes (holistic review, editorial parsing) rely on SSE. Cloudflare can close idle connections. A heartbeat mechanism and DB persistence for results are implemented as safeguards.
- **Re-running Series Arc Verification**: For standalone projects, `runStandaloneArcCheck` is invoked after `finalizeCompletedProject` to audit arc closure.
- **Narrator Header Leakage**: The Ghostwriter agent has defensive sanitization to prevent meta-referential chapter headers from appearing in the generated prose.
- **DeepSeek `temperature`/`top_p`**: When `thinking` is enabled for DeepSeek models, `temperature` and `top_p` parameters are silently ignored.

## Pointers

- **OpenAI-compatible SDK**: Used for all DeepSeek API calls. [DeepSeek API Docs](https://www.deepseek.com/docs)
- **Drizzle ORM**: [Drizzle Documentation](https://orm.drizzle.team/docs/overview)
- **TanStack Query**: [TanStack Query Documentation](https://tanstack.com/query/latest)
- **Tailwind CSS**: [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- **Fish Audio API**: [Fish Audio API Documentation](https://fishaudio.com/docs)